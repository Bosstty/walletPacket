<navigation-bar title="{{id ? '编辑账单' : '记一笔'}}" color="#2F241F" background="transparent"></navigation-bar>

<view class="app-page">
  <view class="page-shell">
    <view class="surface-card form-card" wx:if="{{!loading}}">
      <view class="form-intro">
        <text class="form-title">{{id ? '修正这笔账单' : '快速记录一笔'}}</text>
        <text class="form-copy">先判断收支，再录金额，最后补充分类和时间，整个过程尽量一屏完成。</text>
      </view>

      <view class="type-switch">
        <view class="switch-track">
          <view class="switch-indicator {{type === 'INCOME' ? 'income' : ''}}"></view>
          <view class="switch-item {{type === 'EXPENSE' ? 'active' : ''}}" data-type="EXPENSE" bindtap="handleTypeChange">支出</view>
          <view class="switch-item {{type === 'INCOME' ? 'active' : ''}}" data-type="INCOME" bindtap="handleTypeChange">收入</view>
        </view>
      </view>

      <view class="amount-panel">
        <text class="amount-prefix">¥</text>
        <input class="amount-input" type="digit" placeholder="0.00" value="{{amountInput}}" bindinput="handleAmountInput" />
      </view>
      <view class="quick-amounts">
        <view wx:for="{{quickAmounts}}" wx:key="*this" class="quick-amount-chip" data-amount="{{item}}" bindtap="handleQuickAmount">
          ¥{{item}}
        </view>
      </view>

      <view class="section">
        <view class="section-header">
          <text class="section-title">分类</text>
          <text class="section-tip">{{type === 'EXPENSE' ? '先选主要消费去向' : '先选主要收入来源'}}</text>
        </view>
        <view class="category-grid">
        <view
          wx:for="{{activeCategories}}"
          wx:key="id"
          class="category-chip {{selectedCategoryId === item.id ? 'selected' : ''}}"
          data-id="{{item.id}}"
            bindtap="handleCategoryTap"
          >
            <text class="category-name">{{item.name}}</text>
          </view>
        </view>
      </view>

      <view class="field-list">
        <view class="field-row">
          <text class="field-label">日期</text>
          <picker mode="date" value="{{occurredDate}}" bindchange="handleDateChange">
            <text class="field-value">{{occurredDate}}</text>
          </picker>
        </view>
        <view class="field-row">
          <text class="field-label">时间</text>
          <picker mode="time" value="{{occurredTime}}" bindchange="handleTimeChange">
            <text class="field-value">{{occurredTime}}</text>
          </picker>
        </view>
        <view class="field-row">
          <text class="field-label">计入预算</text>
          <view class="custom-toggle {{budgetIncluded ? 'active' : ''}}" bindtap="handleBudgetToggleTap">
            <view class="custom-toggle__thumb"></view>
          </view>
        </view>
      </view>

      <view class="section">
        <view class="section-header">
          <text class="section-title">备注</text>
          <text class="section-tip">可选，用来标记用途或来源</text>
        </view>
        <view class="quick-notes">
          <view class="quick-note-chip" data-note="早餐" bindtap="handleQuickNote"># 早餐</view>
          <view class="quick-note-chip" data-note="午餐" bindtap="handleQuickNote"># 午餐</view>
          <view class="quick-note-chip" data-note="交通" bindtap="handleQuickNote"># 交通</view>
          <view class="quick-note-chip" data-note="工资" bindtap="handleQuickNote"># 工资</view>
        </view>
        <textarea class="note-area" maxlength="200" placeholder="补充这笔账单的细节" value="{{note}}" bindinput="handleNoteInput"></textarea>
      </view>

      <button class="pill-button save-button" loading="{{saving}}" bindtap="handleSave">
        {{id ? '更新账单' : '保存账单'}}
      </button>

      <button wx:if="{{id}}" class="ghost-button delete-button" bindtap="handleDelete">删除这笔账单</button>
    </view>
  </view>
</view>
